def send_error_audio(chat_id: int):
    path = "render/audio/amharic/error.ogg"
    if os.path.exists(path):
        with open(path, "rb") as audio_file:
            bot.send_audio(chat_id, audio_file)
